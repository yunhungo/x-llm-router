{
  "name": "x-llm-router",
  "version": "0.1.0",
  "private": true,
  "description": "A self-hosted OpenAI-compatible LLM gateway with OAuth, virtual keys, usage analytics, and Langfuse tracing.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.21"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @x-router/api --filter @x-router/web dev",
    "build": "pnpm --filter @x-router/contracts build && pnpm --filter @x-router/api build && pnpm --filter @x-router/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:migrate": "pnpm --filter @x-router/api db:migrate"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
